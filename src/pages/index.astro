---
import Layout from '../layouts/Layout.astro';

const portfolioItems = [
  { id: "Artisan", name: "Site Vitrine — Artisan", desc: "Site moderne et responsive pour un artisan local.", url: "https://boutique-artisan.netlify.app", img: "/Portfolio preview/boutique-artisan.png", tags: ["vitrine", "SEO"] },
  { id: "Restaurant", name: "Site Web — Restaurant", desc: "Menu interactif, réservation en ligne, galerie photos.", url: "https://restaurant-template-regal-authentique.netlify.app/", img: "/Portfolio preview/restaurant-regal-authentique.png", tags: ["réservation", "menu"] },
  { id: "Gite", name: "Location de Gîte", desc: "Calendrier de disponibilités, formulaire de réservation.", url: "https://gite-provence.netlify.app/", img: "/Portfolio preview/gite-provence.png", tags: ["tourisme", "réservation"] },
  { id: "Bolt", name: "Projet Bolt DIY", desc: "Site dynamique avec composants interactifs.", url: "https://bolt-diy-16-1746971425202.netlify.app/", img: "/Portfolio preview/VOID-engine.png", tags: ["interactif", "SPA"] },
];
---

<Layout title="SOS-PC — Dépannage & Solutions Informatiques">

  <!-- ===== HERO ===== -->
  <section id="hero" class="hero-section">
    <div class="hero-terminal">
      <div class="terminal-bar">
        <span class="t-dot red"></span>
        <span class="t-dot yellow"></span>
        <span class="t-dot green"></span>
        <span class="t-title">sos-pc.sh — bash</span>
      </div>
      <div class="terminal-body">
        <div class="t-line"><span class="t-prompt">$</span> <span class="t-cmd">whoami</span></div>
        <div class="t-output">sos-pc — expert en dépannage &amp; solutions numériques</div>
        <div class="t-line"><span class="t-prompt">$</span> <span class="t-cmd">cat services.txt</span></div>
        <div class="t-output t-list">
          <span class="t-item ok">✓ Réparation informatique</span>
          <span class="t-item ok">✓ Création de sites web</span>
          <span class="t-item ok">✓ Hébergement streaming</span>
          <span class="t-item warn">~ Hébergement IA (coming soon)</span>
        </div>
        <div class="t-line"><span class="t-prompt">$</span> <span class="t-cursor">_</span></div>
      </div>
    </div>

    <div class="hero-content">
      <div class="hero-badge">
        <span class="tag tag-teal">SOS-PC</span>
        <span class="hero-location mono">// Le Castellet, 04700</span>
      </div>
      <h1 class="hero-title">
        Votre <em>partenaire tech</em><br/>de confiance
      </h1>
      <p class="hero-sub">
        Diagnostic IA, réparation, création web et self-hosting —<br/>des solutions fiables pour particuliers et pros.
      </p>
      <div class="hero-actions">
        <a href="#diagnostic" class="btn-primary">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>
          Diagnostic gratuit
        </a>
        <a href="#contact" class="btn-ghost">Demander un devis</a>
      </div>
    </div>
  </section>

  <!-- ===== ABOUT ===== -->
  <section id="about" class="about-section">
    <div class="section-label">
      <span class="tag tag-teal">01</span>
      <span class="mono text-muted">// À propos</span>
    </div>
    <div class="about-grid">
      <div>
        <h2>Solutions fiables,<br/><span class="teal">adaptées à vos besoins</span></h2>
        <p>SOS-PC, dirigé par un auto-entrepreneur passionné, propose une gamme complète de services pour répondre à vos besoins technologiques. Particuliers ou professionnels — nous avons l'expertise qu'il vous faut.</p>
        <div class="about-stats">
          <div class="stat">
            <span class="stat-val mono">04+</span>
            <span class="stat-label">Services proposés</span>
          </div>
          <div class="stat">
            <span class="stat-val mono">24h</span>
            <span class="stat-label">Délai de réponse</span>
          </div>
          <div class="stat">
            <span class="stat-val mono">100%</span>
            <span class="stat-label">Sur mesure</span>
          </div>
        </div>
      </div>
      <div class="about-card-stack">
        <div class="info-card">
          <span class="ic-icon">⚡</span>
          <div>
            <p class="ic-title">Intervention rapide</p>
            <p class="ic-desc">Dépannage à distance ou à domicile dans les Alpes-de-Haute-Provence</p>
          </div>
        </div>
        <div class="info-card">
          <span class="ic-icon">🔒</span>
          <div>
            <p class="ic-title">Données sécurisées</p>
            <p class="ic-desc">Vos données restent privées — aucune revente, aucun partage tiers</p>
          </div>
        </div>
        <div class="info-card">
          <span class="ic-icon">🤝</span>
          <div>
            <p class="ic-title">Accompagnement humain</p>
            <p class="ic-desc">Un interlocuteur unique, disponible et à l'écoute</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ===== SERVICES ===== -->
  <section id="services" class="services-section">
    <div class="section-label">
      <span class="tag tag-teal">02</span>
      <span class="mono text-muted">// Services</span>
    </div>
    <h2 class="section-title">Nos <span class="teal">solutions</span></h2>

    <div class="services-grid">

      <div class="service-card-new">
        <div class="sc-header">
          <div class="sc-icon-wrap" style="--ic:#00d4aa">
            <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"><rect x="2" y="3" width="20" height="14" rx="2"/><path d="M8 21h8M12 17v4"/></svg>
          </div>
          <span class="tag tag-teal">disponible</span>
        </div>
        <h3>Réparation<br/>Informatique</h3>
        <p>Diagnostic, dépannage, montage sur mesure et optimisation. Nous redonnons vie à votre matériel.</p>
        <ul class="sc-list">
          <li><span class="mono teal">→</span> Diagnostic &amp; dépannage</li>
          <li><span class="mono teal">→</span> Remplacement de composants</li>
          <li><span class="mono teal">→</span> Montage PC personnalisé</li>
          <li><span class="mono teal">→</span> Optimisation &amp; récupération</li>
        </ul>
        <a href="#contact" class="sc-cta">Demander un devis <span>→</span></a>
      </div>

      <div class="service-card-new">
        <div class="sc-header">
          <div class="sc-icon-wrap" style="--ic:#7aa2f7">
            <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"><rect x="3" y="3" width="18" height="18" rx="2"/><path d="M3 9h18M9 21V9"/></svg>
          </div>
          <span class="tag tag-blue">disponible</span>
        </div>
        <h3>Création de<br/>Sites Web</h3>
        <p>Sites vitrines modernes, responsives et optimisés pour votre présence en ligne.</p>
        <ul class="sc-list">
          <li><span class="mono" style="color:#7aa2f7">→</span> Sites vitrines pro</li>
          <li><span class="mono" style="color:#7aa2f7">→</span> Design responsive</li>
          <li><span class="mono" style="color:#7aa2f7">→</span> SEO de base inclus</li>
          <li><span class="mono" style="color:#7aa2f7">→</span> Fonctionnalités sur mesure</li>
        </ul>
        <a href="#contact" class="sc-cta" style="color:#7aa2f7">Discutons de votre projet <span>→</span></a>
      </div>

      <div class="service-card-new">
        <div class="sc-header">
          <div class="sc-icon-wrap" style="--ic:#bb9af7">
            <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"><path d="M22 12H2M22 12l-4-4M22 12l-4 4M2 12l4-4M2 12l4 4"/><circle cx="12" cy="12" r="3"/></svg>
          </div>
          <span class="tag tag-purple">disponible</span>
        </div>
        <h3>Hébergement<br/>Streaming</h3>
        <p>Votre propre plateforme média (Jellyfin, etc.) accessible partout, en toute sécurité.</p>
        <ul class="sc-list">
          <li><span class="mono" style="color:#bb9af7">→</span> Installation Jellyfin/similaires</li>
          <li><span class="mono" style="color:#bb9af7">→</span> Conseils matériel/serveur</li>
          <li><span class="mono" style="color:#bb9af7">→</span> Accès sécurisé</li>
          <li><span class="mono" style="color:#bb9af7">→</span> Maintenance de base</li>
        </ul>
        <a href="#contact" class="sc-cta" style="color:#bb9af7">En savoir plus <span>→</span></a>
      </div>

      <div class="service-card-new coming-soon">
        <div class="sc-header">
          <div class="sc-icon-wrap" style="--ic:#e0af68">
            <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"><path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5"/></svg>
          </div>
          <span class="tag tag-yellow">coming soon</span>
        </div>
        <h3>Hébergement<br/>Modèles IA</h3>
        <p>Déployez et gérez vos modèles d'IA pour explorer leur potentiel. Service en développement.</p>
        <ul class="sc-list sc-list--dim">
          <li><span class="mono" style="color:#e0af68">→</span> Conseils hébergement</li>
          <li><span class="mono" style="color:#e0af68">→</span> Aide au déploiement</li>
          <li><span class="mono" style="color:#e0af68">→</span> Gestion infrastructure</li>
          <li><span class="mono" style="color:#e0af68">→</span> Contactez-nous pour discuter</li>
        </ul>
        <a href="#contact" class="sc-cta" style="color:#e0af68">Contactez-nous <span>→</span></a>
      </div>

    </div>
  </section>

  <!-- ===== DIAGNOSTIC ===== -->
  <section id="diagnostic" class="diag-section">
    <div class="section-label">
      <span class="tag tag-teal">03</span>
      <span class="mono text-muted">// Diagnostic IA</span>
    </div>
    <div class="diag-grid">
      <div class="diag-content">
        <h2>Diagnostic PC<br/><span class="teal">Intelligent</span></h2>
        <p>Notre IA analyse en profondeur votre système Windows — CPU, RAM, disques, erreurs système — et vous propose un rapport clair avec des recommandations concrètes.</p>
        <div class="diag-features">
          <div class="df-item"><span class="teal mono">✓</span> Analyse complète en 30 secondes</div>
          <div class="df-item"><span class="teal mono">✓</span> Rapport détaillé avec priorités</div>
          <div class="df-item"><span class="teal mono">✓</span> Chat IA pour aller plus loin</div>
          <div class="df-item"><span class="teal mono">✓</span> 100% gratuit, aucune installation</div>
        </div>
        <div class="diag-privacy">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>
          Aucun fichier personnel collecté — uniquement les données techniques système.
        </div>
      </div>
      <div class="diag-terminal">
        <div class="terminal-bar">
          <span class="t-dot red"></span>
          <span class="t-dot yellow"></span>
          <span class="t-dot green"></span>
          <span class="t-title">diagnostic-sos-pc.ps1</span>
        </div>
        <div class="terminal-body">
          <div class="t-line"><span class="t-prompt">PS&gt;</span> <span class="t-cmd">$s='XXXX'; irm sos-pc.click/diag.ps1 | iex</span></div>
          <div class="t-output">Collecte des informations système...</div>
          <div class="t-output t-list">
            <span class="t-item ok">✓ OS Windows 11 Pro</span>
            <span class="t-item ok">✓ CPU Intel i7 — 23% load</span>
            <span class="t-item ok">✓ RAM 16 Go — 68% utilisée</span>
            <span class="t-item warn">⚠ Disque C: 94% utilisé</span>
            <span class="t-item err">✗ 3 erreurs critiques détectées</span>
          </div>
          <div class="t-output teal">Envoi des données vers SOS-PC...</div>
          <div class="t-output" style="color:#00d4aa;font-weight:600">✓ Retournez sur sos-pc.click !</div>
        </div>
        <div class="diag-cta-box">
          <p class="mono" style="font-size:12px;color:var(--text-muted);margin-bottom:8px;">
            // Ouvrez le chatbot → copiez la commande → collez dans PowerShell
          </p>
          <button class="btn-primary" onclick="if(!window.isOpen){sospcToggle()}" style="width:100%;justify-content:center;">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/></svg>
            Lancer le diagnostic
          </button>
        </div>
      </div>
    </div>
  </section>

  <!-- ===== LOCATION ===== -->
  <section id="location" class="location-section">
    <div class="section-label">
      <span class="tag tag-teal">04</span>
      <span class="mono text-muted">// Zone d'intervention</span>
    </div>
    <h2 class="section-title">Zone d'intervention</h2>
    <p class="section-sub">Basé près de Le Castellet (04700), nous intervenons dans les environs.</p>
    <div class="map-wrapper">
      <iframe title="Zone intervention SOS-PC" src="https://maps.google.com/maps?q=Le+Castellet,+04700&output=embed" loading="lazy" style="border:0;width:100%;height:340px;border-radius:var(--radius-lg);" allowfullscreen></iframe>
    </div>
  </section>

  <!-- ===== PORTFOLIO ===== -->
  <section id="vitrine" class="portfolio-section">
    <div class="section-label">
      <span class="tag tag-teal">05</span>
      <span class="mono text-muted">// Réalisations</span>
    </div>
    <h2 class="section-title">Notre <span class="teal">vitrine</span></h2>
    <div class="portfolio-grid">
      {portfolioItems.map(item => (
        <a href={item.url} target="_blank" rel="noopener" class="portfolio-card">
          <div class="pc-img-wrap">
            <img src={item.localFallbackImg || item.img} alt={item.name} loading="lazy" />
            <div class="pc-overlay">
              <span class="mono" style="font-size:12px;color:var(--accent-hex)">Voir le site →</span>
            </div>
          </div>
          <div class="pc-info">
            <div class="pc-tags">
              {item.tags.map(t => <span class="tag tag-teal" style="font-size:10px">{t}</span>)}
            </div>
            <h3>{item.name}</h3>
            <p>{item.desc}</p>
          </div>
        </a>
      ))}
    </div>
  </section>

  <!-- ===== CONTACT ===== -->
  <section id="contact" class="contact-section">
    <div class="section-label">
      <span class="tag tag-teal">06</span>
      <span class="mono text-muted">// Contact</span>
    </div>
    <div class="contact-grid">
      <div>
        <h2>Contactez<br/><span class="teal">SOS-PC</span></h2>
        <p>Pour toute demande d'information ou devis, remplissez le formulaire ou contactez-nous directement.</p>
        <div class="contact-links">
          <a href="tel:+33769561491" class="contact-link">
            <span class="cl-icon mono">📞</span>
            <div><span class="cl-label mono">Téléphone</span><span class="cl-val">07 69 56 14 91</span></div>
          </a>
          <a href="mailto:sos.pc.04@gmail.com" class="contact-link">
            <span class="cl-icon mono">✉</span>
            <div><span class="cl-label mono">Email</span><span class="cl-val">sos.pc.04@gmail.com</span></div>
          </a>
          <a href="https://discord.gg/APFtmK5PYc" target="_blank" class="contact-link">
            <span class="cl-icon mono">💬</span>
            <div><span class="cl-label mono">Discord</span><span class="cl-val">Rejoindre le serveur</span></div>
          </a>
        </div>
      </div>
      <form class="contact-form" name="contact" method="POST" data-netlify="true" netlify-honeypot="bot-field">
        <input type="hidden" name="form-name" value="contact" />
        <p style="display:none"><label>Bot: <input name="bot-field" /></label></p>
        <div class="form-row">
          <div class="form-group">
            <label class="mono">nom</label>
            <input type="text" name="name" required placeholder="Votre nom" />
          </div>
          <div class="form-group">
            <label class="mono">email</label>
            <input type="email" name="email" required placeholder="votre@email.com" />
          </div>
        </div>
        <div class="form-group">
          <label class="mono">sujet</label>
          <input type="text" name="subject" placeholder="Objet de votre message" />
        </div>
        <div class="form-group">
          <label class="mono">message</label>
          <textarea name="message" rows="4" required placeholder="Décrivez votre besoin..."></textarea>
        </div>
        <button type="submit" class="btn-primary" style="width:100%;justify-content:center;">
          Envoyer le message
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="22" y1="2" x2="11" y2="13"/><polygon points="22 2 15 22 11 13 2 9 22 2"/></svg>
        </button>
      </form>
    </div>
  </section>

</Layout>

<style>
  /* ===========================
     SHARED
  =========================== */
  .teal { color: var(--accent-hex); }
  .text-muted { color: var(--text-muted); }
  .section-label { display: flex; align-items: center; gap: 10px; margin-bottom: 1rem; }
  .section-title { font-size: clamp(2rem, 4vw, 2.8rem); margin-bottom: 0.75rem; }
  .section-sub { color: var(--text-muted); font-size: 15px; margin-bottom: 2rem; }

  /* ===========================
     HERO
  =========================== */
  .hero-section {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
    min-height: calc(100vh - 80px);
    padding: 5rem 0;
    border-bottom: 1px solid var(--border);
  }

  .hero-content { order: -1; }
  .hero-terminal { order: 1; }

  .hero-badge { display: flex; align-items: center; gap: 12px; margin-bottom: 1.5rem; }
  .hero-location { font-size: 12px; color: var(--text-dim); }

  .hero-title {
    font-size: clamp(2.4rem, 5vw, 3.6rem);
    margin-bottom: 1.25rem;
    line-height: 1.1;
  }
  .hero-title em {
    font-style: normal;
    color: var(--accent-hex);
  }
  .hero-sub { color: var(--text-muted); font-size: 16px; margin-bottom: 2rem; line-height: 1.6; }
  .hero-actions { display: flex; gap: 12px; flex-wrap: wrap; }

  /* Terminal component */
  .terminal-bar {
    display: flex; align-items: center; gap: 6px;
    background: var(--bg-elevated); padding: 10px 16px;
    border-radius: var(--radius-lg) var(--radius-lg) 0 0;
    border-bottom: 1px solid var(--border);
  }
  .t-dot { width: 12px; height: 12px; border-radius: 50%; }
  .t-dot.red { background: #f7768e; }
  .t-dot.yellow { background: #e0af68; }
  .t-dot.green { background: #00d4aa; }
  .t-title { font-family: var(--font-mono); font-size: 12px; color: var(--text-muted); margin-left: 8px; }

  .terminal-body {
    background: #0d0e14;
    border-radius: 0 0 var(--radius-lg) var(--radius-lg);
    padding: 20px;
    font-family: var(--font-mono);
    font-size: 13px;
    line-height: 1.8;
    border: 1px solid var(--border);
    border-top: none;
  }
  .hero-terminal .terminal-body { border-radius: var(--radius-lg); border-top: 1px solid var(--border); }
  .hero-terminal { border-radius: var(--radius-lg); border: 1px solid var(--border); overflow: hidden; box-shadow: 0 0 30px rgba(0,0,0,0.4), 0 0 0 1px rgba(0,212,170,0.05); }

  .t-line { display: flex; gap: 8px; align-items: center; }
  .t-prompt { color: var(--accent-hex); font-weight: 600; }
  .t-cmd { color: var(--text); }
  .t-output { color: var(--text-muted); padding-left: 16px; }
  .t-list { display: flex; flex-direction: column; gap: 2px; }
  .t-item { display: block; }
  .t-item.ok { color: #00d4aa; }
  .t-item.warn { color: #e0af68; }
  .t-item.err { color: #f7768e; }
  .t-cursor { color: var(--accent-hex); animation: blink 1s step-end infinite; }
  @keyframes blink { 0%,100%{opacity:1} 50%{opacity:0} }

  /* ===========================
     ABOUT
  =========================== */
  .about-section { padding: 5rem 0; }
  .about-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 4rem; align-items: start; margin-top: 2rem; }
  .about-grid h2 { font-size: clamp(1.8rem, 3.5vw, 2.4rem); margin-bottom: 1rem; line-height: 1.2; }
  .about-grid p { color: var(--text-muted); line-height: 1.7; margin-bottom: 2rem; }

  .about-stats { display: grid; grid-template-columns: repeat(3, 1fr); gap: 16px; }
  .stat { background: var(--bg-card); border: 1px solid var(--border); border-radius: var(--radius); padding: 16px; }
  .stat-val { display: block; font-size: 1.8rem; font-weight: 700; color: var(--accent-hex); line-height: 1; margin-bottom: 4px; }
  .stat-label { font-size: 12px; color: var(--text-muted); }

  .about-card-stack { display: flex; flex-direction: column; gap: 12px; }
  .info-card {
    display: flex; align-items: flex-start; gap: 14px;
    background: var(--bg-card); border: 1px solid var(--border);
    border-radius: var(--radius); padding: 16px;
    transition: border-color 0.2s, box-shadow 0.2s;
  }
  .info-card:hover { border-color: rgba(0,212,170,0.4); box-shadow: 0 0 16px rgba(0,212,170,0.08); }
  .ic-icon { font-size: 20px; flex-shrink: 0; margin-top: 2px; }
  .ic-title { font-size: 14px; font-weight: 600; color: var(--text); margin-bottom: 3px; }
  .ic-desc { font-size: 13px; color: var(--text-muted); line-height: 1.4; }

  /* ===========================
     SERVICES
  =========================== */
  .services-section { padding: 5rem 0; }
  .services-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 20px; margin-top: 2.5rem; }

  .service-card-new {
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    padding: 24px;
    display: flex; flex-direction: column; gap: 12px;
    transition: all 0.25s;
    position: relative;
    overflow: hidden;
  }
  .service-card-new::before {
    content: '';
    position: absolute; top: 0; left: 0; right: 0; height: 2px;
    background: var(--ic, var(--accent-hex));
    opacity: 0;
    transition: opacity 0.25s;
  }
  .service-card-new:hover { border-color: rgba(255,255,255,0.1); transform: translateY(-2px); box-shadow: 0 8px 24px rgba(0,0,0,0.3); }
  .service-card-new:hover::before { opacity: 1; }
  .service-card-new.coming-soon { opacity: 0.75; }

  .sc-header { display: flex; align-items: center; justify-content: space-between; margin-bottom: 4px; }
  .sc-icon-wrap {
    width: 40px; height: 40px; border-radius: var(--radius);
    background: rgba(from var(--ic) r g b / 0.12);
    border: 1px solid rgba(from var(--ic) r g b / 0.25);
    display: flex; align-items: center; justify-content: center;
    color: var(--ic);
  }
  .service-card-new h3 { font-size: 1.2rem; font-weight: 700; color: var(--text); line-height: 1.2; }
  .service-card-new p { font-size: 13px; color: var(--text-muted); line-height: 1.6; }

  .sc-list { list-style: none; display: flex; flex-direction: column; gap: 6px; flex: 1; }
  .sc-list li { font-size: 13px; color: var(--text-muted); display: flex; gap: 8px; }
  .sc-list--dim li { opacity: 0.6; }

  .sc-cta { font-size: 13px; font-family: var(--font-mono); color: var(--accent-hex); font-weight: 500; margin-top: auto; transition: gap 0.2s; }
  .sc-cta:hover { opacity: 1; }
  .sc-cta span { margin-left: 4px; }

  /* ===========================
     DIAGNOSTIC
  =========================== */
  .diag-section { padding: 5rem 0; }
  .diag-grid { display: grid; grid-template-columns: 1fr 1.1fr; gap: 4rem; align-items: start; margin-top: 2rem; }

  .diag-content h2 { font-size: clamp(1.8rem, 3.5vw, 2.4rem); margin-bottom: 1rem; }
  .diag-content p { color: var(--text-muted); line-height: 1.7; margin-bottom: 1.5rem; }

  .diag-features { display: flex; flex-direction: column; gap: 8px; margin-bottom: 1.5rem; }
  .df-item { font-size: 14px; color: var(--text); display: flex; gap: 10px; }

  .diag-privacy {
    display: flex; align-items: center; gap: 8px;
    font-size: 12px; color: var(--text-dim); font-family: var(--font-mono);
    background: rgba(0,212,170,0.05); border: 1px solid rgba(0,212,170,0.15);
    border-radius: var(--radius); padding: 8px 12px;
  }

  .diag-terminal { border-radius: var(--radius-lg); border: 1px solid var(--border); overflow: hidden; box-shadow: 0 0 30px rgba(0,0,0,0.4); }
  .diag-cta-box { padding: 16px; background: var(--bg-elevated); border-top: 1px solid var(--border); }

  /* ===========================
     LOCATION
  =========================== */
  .location-section { padding: 5rem 0; }
  .map-wrapper { border-radius: var(--radius-lg); overflow: hidden; border: 1px solid var(--border); }

  /* ===========================
     PORTFOLIO
  =========================== */
  .portfolio-section { padding: 5rem 0; }
  .portfolio-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 20px; margin-top: 2.5rem; }

  .portfolio-card {
    background: var(--bg-card); border: 1px solid var(--border);
    border-radius: var(--radius-lg); overflow: hidden;
    text-decoration: none; display: block;
    transition: all 0.25s;
  }
  .portfolio-card:hover { border-color: rgba(0,212,170,0.4); transform: translateY(-2px); box-shadow: 0 8px 24px rgba(0,0,0,0.3), 0 0 0 1px rgba(0,212,170,0.1); opacity: 1; }

  .pc-img-wrap { position: relative; overflow: hidden; height: 180px; }
  .pc-img-wrap img { width: 100%; height: 100%; object-fit: cover; transition: transform 0.4s; filter: saturate(0.8); }
  .portfolio-card:hover .pc-img-wrap img { transform: scale(1.04); filter: saturate(1); }
  .pc-overlay {
    position: absolute; inset: 0; background: rgba(13,14,20,0.7);
    display: flex; align-items: center; justify-content: center;
    opacity: 0; transition: opacity 0.25s;
  }
  .portfolio-card:hover .pc-overlay { opacity: 1; }

  .pc-info { padding: 16px; }
  .pc-tags { display: flex; gap: 6px; margin-bottom: 8px; flex-wrap: wrap; }
  .pc-info h3 { font-size: 14px; font-weight: 600; color: var(--text); margin-bottom: 4px; }
  .pc-info p { font-size: 12px; color: var(--text-muted); line-height: 1.5; }

  /* ===========================
     CONTACT
  =========================== */
  .contact-section { padding: 5rem 0; }
  .contact-grid { display: grid; grid-template-columns: 1fr 1.4fr; gap: 4rem; align-items: start; margin-top: 2rem; }
  .contact-grid h2 { font-size: clamp(1.8rem, 3.5vw, 2.4rem); margin-bottom: 1rem; }
  .contact-grid > div > p { color: var(--text-muted); margin-bottom: 2rem; line-height: 1.7; }

  .contact-links { display: flex; flex-direction: column; gap: 10px; }
  .contact-link {
    display: flex; align-items: center; gap: 14px;
    background: var(--bg-card); border: 1px solid var(--border);
    border-radius: var(--radius); padding: 14px 16px;
    text-decoration: none; transition: all 0.2s;
  }
  .contact-link:hover { border-color: rgba(0,212,170,0.4); background: var(--bg-elevated); opacity: 1; }
  .cl-icon { font-size: 18px; flex-shrink: 0; }
  .cl-label { display: block; font-size: 10px; color: var(--text-dim); text-transform: uppercase; letter-spacing: 0.08em; margin-bottom: 2px; }
  .cl-val { display: block; font-size: 14px; color: var(--text); font-weight: 500; }

  .contact-form { display: flex; flex-direction: column; gap: 14px; }
  .form-row { display: grid; grid-template-columns: 1fr 1fr; gap: 14px; }
  .form-group { display: flex; flex-direction: column; gap: 5px; }
  .form-group label { font-size: 11px; color: var(--text-muted); letter-spacing: 0.05em; }
  .form-group input, .form-group textarea {
    background: var(--bg-card); border: 1px solid var(--border);
    border-radius: var(--radius); padding: 10px 14px;
    font-size: 14px; color: var(--text); font-family: var(--font-body);
    outline: none; transition: all 0.2s;
  }
  .form-group input:focus, .form-group textarea:focus {
    border-color: var(--accent-hex);
    box-shadow: 0 0 0 3px rgba(0,212,170,0.1);
  }
  .form-group textarea { resize: vertical; min-height: 100px; }

  /* ===========================
     RESPONSIVE
  =========================== */
  @media (max-width: 900px) {
    .hero-section { grid-template-columns: 1fr; min-height: auto; }
    .hero-content { order: 0; }
    .hero-terminal { order: 1; }
    .about-grid, .diag-grid, .contact-grid { grid-template-columns: 1fr; gap: 2rem; }
    .services-grid, .portfolio-grid { grid-template-columns: 1fr; }
    .about-stats { grid-template-columns: repeat(3, 1fr); }
    .form-row { grid-template-columns: 1fr; }
  }
  @media (max-width: 480px) {
    .about-stats { grid-template-columns: 1fr 1fr; }
    .hero-actions { flex-direction: column; }
  }
</style>

<script>
  // Animation d'apparition au scroll
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('visible');
        observer.unobserve(entry.target);
      }
    });
  }, { threshold: 0.1 });

  document.querySelectorAll('section, .service-card-new, .portfolio-card, .info-card').forEach(el => {
    el.style.opacity = '0';
    el.style.transform = 'translateY(20px)';
    el.style.transition = 'opacity 0.5s ease, transform 0.5s ease';
    observer.observe(el);
  });

  document.addEventListener('DOMContentLoaded', () => {
    document.querySelectorAll('section, .service-card-new, .portfolio-card, .info-card').forEach(el => {
      el.classList.add = (cls) => {
        if (cls === 'visible') {
          el.style.opacity = '1';
          el.style.transform = 'translateY(0)';
        }
      };
    });
  });

  // Scroll reveal simplifié
  const revealObserver = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.style.opacity = '1';
        entry.target.style.transform = 'translateY(0)';
      }
    });
  }, { threshold: 0.08, rootMargin: '0px 0px -40px 0px' });

  document.querySelectorAll('section, .service-card-new, .portfolio-card, .info-card, .stat').forEach((el, i) => {
    el.style.opacity = '0';
    el.style.transform = 'translateY(24px)';
    el.style.transition = `opacity 0.55s ease ${i * 0.04}s, transform 0.55s ease ${i * 0.04}s`;
    revealObserver.observe(el);
  });
</script>
